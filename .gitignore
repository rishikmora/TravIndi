# dependencies
/node_modules

# next.js
/.next/
/out/
next-env.d.ts
*.tsbuildinfo

# production
/build

# env files (keep .env.example)
.env
.env.*
!.env.example
# Public demo flags only (no secrets) — see the file itself.
!.env.production

# debug
npm-debug.log*

# misc
.DS_Store
Thumbs.db
