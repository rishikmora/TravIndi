"""Analytics domain — aggregates, KPI snapshots, feature tables.
Owns the `analytics` DB schema.

P1. Only named at the schema level in the source documents — no field-level
detail exists anywhere; built out as concrete analytics requirements emerge
post-MVP.
"""
