# Local dev: the FastAPI backend's URL. Copy to .env.local.
# On Vercel: set NEXT_PUBLIC_API_BASE_URL as a Project env var to your
# deployed backend's HTTPS URL instead — see ../DEPLOYMENT.md.
NEXT_PUBLIC_API_BASE_URL=http://localhost:8010
