# Recommended Implementation Order — Phase 6 onward

Status: **DESIGNED**. Phases 1–5 (this directory) are complete. This is the
recommended order for Phase 6–22 per the governing workflow, adapted to the frozen P0
slice in [04-feature-priority-matrix.md](04-feature-priority-matrix.md) and
[05-traceability-matrix-mvp.md](05-traceability-matrix-mvp.md).

**Strategic pivot, 2026-09-07 (after Phase 12):** the user redirected subsequent work
from "one fully-verified phase at a time" to "a working SIH prototype covering every
frozen P0 feature, at prototype depth where full depth isn't needed to demo the
behavior." Phases 13/14/16 and part of 15 were built in a single pass under that
directive — each row below states explicitly which pieces are full-depth vs.
prototype-depth-by-design, so "done" never silently means "faked." Phases 6–12 were
built under the original fully-verified approach and are unaffected by this note.

| Phase | Scope | Key open decisions to resolve first |
|---|---|---|
| 6. Repository / project structure — **DONE 2026-09-07** | `backend/` (FastAPI skeleton, 14 domain-module packages mirroring the schema plan, health check verified via pytest, ruff-clean), `mobile/` + `web/` (documented placeholders, real scaffolding deferred to Phase 11 since there's no API yet to build against), `infra/` (Docker Compose: Postgres+PostGIS+pgvector via custom Dockerfile, Redis, MinIO always-on; Keycloak+Kong behind a `full` profile for Phase 9+), git initialized (not yet committed — no commit has been made without an explicit request) | Cloud provider not yet chosen — not needed yet; Docker Compose is sufficient |
| 7. Database migrations & seed strategy — **DONE 2026-09-07 (P0 schemas only)** | SQLAlchemy models + Alembic migrations for the 8 P0 schemas (identity, tourism, travel, safety, emergency, crowd, knowledge, governance — 44 tables), verified end-to-end against a live Postgres: migration applies cleanly, downgrade/re-upgrade round-trips correctly, RLS is genuinely enforced (tested with a real non-superuser role, not just declared), idempotent seed script runs separately from migrations. business/booking/payment/trust/analytics/integration (P1+) have schemas only, no tables yet — built when those phases are reached. | None blocking |
| 8. API contracts — **DONE 2026-09-07** | All 35 P0 endpoints implemented as real FastAPI routes with Pydantic request/response schemas, canonical error envelope, request-id correlation, and cursor pagination — verified via a running server (curl) and 16 passing tests (unit + integration). Destinations/crowd endpoints are genuinely DB-backed (Phase 7 tables); everything requiring auth/AI/GIS/SOS-dispatch/sync/notifications is an honest `501 NOT_IMPLEMENTED_YET` contract stub naming the phase that implements it — never a fake success. Canonical paths per Conflict Register §4/§5 (`/api/v1/routes/{mode}`, `/api/v1/emergency/incidents/*`). | None blocking |
| 9. Authentication / authorization — **DONE 2026-09-07** | Real Keycloak realm (`travindi`) with the 9 confirmed roles + 2 clients, imported automatically via Docker; real JWT verification (signature/issuer/audience/expiry) replacing the Phase 8 stub; `/auth/login` + `/auth/token/refresh` proxy to Keycloak for real; OPA running with a Rego policy encoding the full confirmed role-permission matrix; an `get_rls_session` dependency wiring the RLS session-variable contract from Phase 7. Verified with 28 new integration tests (real tokens, real OPA decisions, real RLS row filtering) — not just written. `/auth/register` and `/auth/otp/verify` stay 501 (need Phase 10's DB-backed User module / an OTP channel, not auth infra). Trusted-contact token flow (§1 of that doc) still adds one table + one endpoint to Phase 14 scope. | None blocking |
| 10. Backend foundations — **DONE 2026-09-07** | Auth & Users real (register via Keycloak Admin API service account + local DB rows, `/users/me`, consent grant/list/revoke); Travel trip CRUD real (OPA-checked ownership on get/update/cancel); Tourism was already real as of Phase 8. `/auth/otp/verify` stays unscheduled (no "request OTP" endpoint in any of the 7 source docs — needs its own design pass, not just implementation time). Verified with 50 passing tests + live-server smoke tests, not just written. Found and fixed two real bugs along the way: a Starlette `TestClient` deadlock on handlers with outbound HTTP calls (whole test suite migrated to `httpx.AsyncClient`+`ASGITransport`), and ~42 timezone-naive datetime columns across every domain model (now `DateTime(timezone=True)` + `eager_defaults` on `TimestampMixin`). | None blocking |
| 11. Frontend foundations — **DONE 2026-09-07** | `web/` (Next.js 16 App Router) and `mobile/` (Expo Router / React Native, SDK 57) both scaffolded and covering: register, login, logout, destination discovery (list + detail + attractions), trip create/list. Route protection verified to actually block navigation, not just hide links — `RequireAuth` (client redirect) on web, `Stack.Protected` (route not registered while logged out) on mobile. Verified live against the real Phase 8-10 backend and real Keycloak/OPA — registered real users, created real trips, logged out and back in, confirmed data persisted, on both apps (mobile verified via `expo start --web`; no simulator available in this environment). Two real bugs found and fixed: backend `.env` resolution broke under `--app-dir` launches (relative path resolved against process cwd, not the module — fixed with an absolute path derived from `config.py`'s own location); expo-router's `Slot` crashed when a `Link`'s `asChild` child received an array of styles (`[styles.button, styles.primaryButton]`) — fixed by flattening to single style objects. AI planning, safe routing, SOS, and offline support are out of scope here per the roadmap. | None blocking |
| 12. AI / RAG — **DONE 2026-09-07** | AI Gateway (`backend/app/core/ai/gateway.py`: API-key config-error checks, real token/cost tracking), Model Router (single Anthropic `claude-sonnet-5` route today, structured for a second later), a real planner agent (`backend/app/domains/travel/planner.py`) grounded via hand-rolled RAG (embed with local `sentence-transformers`, cosine-search pgvector, no LlamaIndex/LangChain — Assumption C2's default was surfaced to the user and overridden: the actual scope is one embedding call + one SQL query). Guardrails are real: every returned attraction id is checked against the DB-fetched candidate set (a hallucinated id is a hard 502), and no-destination requests fail closed with 422 rather than guessing. Three endpoints went from Phase 8 501-stubs to real: `POST /ai/trip-plan`, `POST /ai/itinerary/generate` (new — the traceability matrix specified it but Phase 8 never stubbed it), `POST /ai/itinerary/{id}/replan`. Verified live: 59/59 tests pass (5 new, real Anthropic API calls, not mocked) plus a live curl smoke test producing a genuinely grounded 2-stop Mumbai itinerary; `knowledge.ai_sessions/ai_messages/ai_tool_calls/ai_predictions` confirmed populated with real token counts/cost/model version after the smoke test. The embedding provider changed mid-implementation — originally planned as OpenAI (1536-dim, matching the Phase 7 placeholder column), switched to local `sentence-transformers` (384-dim) when no OpenAI/Voyage key was available; a new migration resizes the column (the table held no real data yet, so no re-embedding was needed). Not built: ROUTE_SEGMENT items (Phase 13), real attraction pricing (needs the unbuilt P1 business/booking schema), async job dispatch for plans exceeding the ≤5s synchronous NFR target (Phase 14). | None blocking |
| 13. GIS / navigation — **DONE 2026-09-07 (prototype depth)** | Real `/routes/safe\|crowd-free\|accessible\|emergency` (`backend/app/domains/travel/routing.py`): a genuine PostGIS query (`ST_DWithin` against a real `ST_GeogFromText` line) scores real nearby open incidents + real nearby crowd risk, weighted per mode, never a placeholder. Deliberately NOT a road-network engine — geometry is a straight line, "along the route" is a fixed-radius corridor buffer, and confidence is derived from whether real crowd data was found nearby (0.8) vs. not (0.5), never fabricated. H3 aggregation was already real since Phase 7/8 (`crowd.crowd_cells`). | **Routing-engine pick (Assumption C1) deliberately deferred, not resolved** — a real OSRM/GraphHopper/Valhalla instance is exactly the kind of infra depth this prototype pass skips; the straight-line heuristic demos the safety-routing *behavior* without it. Revisit if road-accurate geometry is ever needed. |
| 14. Safety / SOS — **DONE 2026-09-07 (prototype depth)** | Real SOS state machine (CREATED → ACKNOWLEDGED → RESOLVED\|FALSE_ALARM, plus owner CANCELLED) and real incident lifecycle (REPORTED → ASSIGNED → IN_PROGRESS → RESOLVED\|FALSE_ALARM\|CANCELLED) — `backend/app/domains/emergency/router.py`, `backend/app/domains/safety/router.py`. Real trusted-contact token issuance (on SOS create) and verification (hash lookup, expiry, revocation, single-use, OTP step-up) — `identity.trusted_contact_access_tokens`, already migrated at Phase 7. Real in-app notifications fired on authority acknowledge/resolve/assign (new `identity.notifications` table + `app/core/notify.py`). Simplified vs. full depth: no WebSocket `/ws/sos/{id}` (the web UI polls instead — a real, working choice, not a placeholder); no real SMS/OTP channel (Assumption C6 — tokens/codes are handled directly rather than "sent"); `assign` self-assigns the acting officer rather than routing to an arbitrary target officer; only CREATED/ACKNOWLEDGED/RESOLVED/CANCELLED/FALSE_ALARM are exposed as transitions (AUTHORITY_NOTIFIED/RESOURCE_ASSIGNED/RESPONDER_ARRIVED exist in the enum for later depth). Verified live in two real browser sessions (tourist + police): SOS created → acknowledged → tourist received the real notification — not just pytest. | Depends on 9, 10 — both real |
| 15. Offline synchronization — **PARTIAL 2026-09-07 (prototype depth, one entity type)** | Real, not stubbed, for exactly the demo-critical path: a `POST /sync` operation with `entity_type: "sos"`, `operation: "CREATE"` genuinely creates a real SOS request, idempotent on `operation_id` (checked against `SosEvent` payloads, same pattern as `POST /sos`'s own Idempotency-Key). Every other entity_type is honestly returned in `skipped_operation_ids`, never silently dropped or faked as acknowledged. No SQLite local store, connectivity manager, or generic multi-entity sync engine — those remain genuinely unbuilt. | Depends on 10, 14 — both real for the "sos" path |
| 16. Authority dashboard — **DONE 2026-09-07 (prototype depth)** | Real `GET /authority/dashboard` (`backend/app/api/v1/authority.py`): live aggregate counts (active SOS, open incidents, high-risk crowd cells ≥0.7) via real DB queries, gated to the roles the RLS read policies actually grant visibility to. Real web UI (`web/src/app/authority/page.tsx`): dashboard tiles + live SOS/incident lists with acknowledge/resolve/assign buttons that call the real Phase 14 endpoints. No dedicated `/ws/authority/{id}` push channel — the page polls/refetches instead. | Depends on 9, 14 — both real |
| 17. Observability | OpenTelemetry, Prometheus/Grafana, Loki, trace correlation across the whole P0 slice | Depends on 10–16 being in place to instrument |
| 18. Tests | Unit, integration, contract, E2E (the core demo journey), load (k6), security, AI eval, offline sync, failure/recovery, migration tests | Provisional NFR placeholders ([Assumption C3](03-assumption-register.md)) used only for automated test thresholds, never reported as verified NFR compliance |
| 19. Security review | OWASP checks, dependency/secrets scanning, RLS boundary tests, rate-limit tuning from Conflict Register §11's provisional numbers | None blocking |
| 20. NFR / load evaluation | Run load tests, **measure** real p50/p95/p99, capacity ceilings, SOS ack latency, reconnect time — this is where NFR's deferred numbers actually get frozen | **User checkpoint: present measured numbers before calling any NFR "met"** |
| 21. Fix defects | — | — |
| 22. Final consistency audit | Re-run the Part 3 audit in [06-architecture-audit-and-gaps.md](06-architecture-audit-and-gaps.md) against the as-built system | — |

## User checkpoints (do not proceed past these without explicit confirmation)

1. ~~Before Phase 7 — confirm the reconciled 14-schema database plan~~ **Done 2026-09-07** — see [09-database-schema-plan.md](09-database-schema-plan.md).
2. ~~Before Phase 9 — confirm the role-permission matrix~~ **Done 2026-09-07** — see [08-role-permission-matrix.md](08-role-permission-matrix.md).
3. ~~Before Phase 12 — confirm RAG library and LLM/embedding provider picks~~
   **Done 2026-09-07** — see [03-assumption-register.md](03-assumption-register.md) C2
   (hand-rolled RAG, no framework; Anthropic for generation, local
   `sentence-transformers` for embeddings). The routing-engine pick (C1) was
   **deliberately deferred, not resolved**, per the 2026-09-07 strategic pivot to
   prototype depth — Phase 13 shipped a real PostGIS-scored straight-line heuristic
   instead of standing up OSRM. Revisit C1 for real if road-accurate geometry is
   ever needed.
4. **Before Phase 20 sign-off** — present actually-measured NFR numbers; do not let
   provisional placeholders quietly become "the spec."

## What is explicitly NOT being built in this pass

Every P1/P2/P3 item in [04-feature-priority-matrix.md](04-feature-priority-matrix.md) —
including crowd forecasting ML, multilingual voice, business/guide verification,
IoT/MQTT ingestion, ticketing/booking/payments, and everything in Regional/National
scope (Kafka, Kubernetes, ClickHouse, digital twin, national knowledge graph). These
remain fully catalogued and staged, not deleted, per the no-silent-change rule.
